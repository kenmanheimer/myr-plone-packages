default_context:
    initial_user_name: 'admin'
    initial_user_password: 'admin'

    load_zcml:
        package_includes: [
            'myr_plone_packages',
            'eea.kitkat',
        ]

    db_storage: direct
